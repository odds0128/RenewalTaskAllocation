・エージェント数などのパラメータはどうやり取りするか?
　グローバルにしてみんなで共有するのかGeneralManagerから明示的に渡すか
　→　散らばるのは嫌なので後者で
　→　無謀でした．大人しく共有させます．ただし最小限で頑張る

・エージェントは自分の座標を知っている必要はないか? チーム編成の際はその辺は全部マネージャに任せる
　→　試しにエージェントに座標をもたせずにやって見る

・タスクやサブタスクはエージェントが持つべきなのかそれともStrategyが持つべきなのか．
　→　どちらを持つかはRole依存なのでRoleの下に持たせる．
　　RoleにもStrategyにも依存しないものだけAgentに持たせる．

・AgentManagerはタスクの終了時や廃棄時にTaskManagerクラスのdisposeTaskやfinishTaskを呼ぶが，そのさらに中からOutPutManagerを読んで集計するようにする
　→　TaskManagerを経由してOutPutManagerに渡す方針にする．あくまで，出力する予定のパラメータは可能な限りOutPutManagerに管理させる

・LeaderクラスとMemberクラスを用意し，役割に依存するパラメータはそこに持たせる．
　→　持たせられない．Leader, Memberには動きだけ記述して逐次値は渡す．

・相互のやり取りはパッケージ内のことであろうと全部Managerを通して行う

・Deliveryクラス要らんやんHashMapでええわ

・Strategies.strategyX.java内にLeaderクラスとMemberクラスの両方押し込むか，
　それともStrategyパッケージ内にLeaderクラスとMemberクラスを作るか?
　なんにせよStrategyによって各Roleの動きは変わるので，Strategyの内部にRoleを置くのが賢そう．
　後者はあまり好まれないやり方なので前者か...?
　命名とかに気をつけて使えばいいというなら後者の方が好みなのだが
　→　パッケージで区別します

? Roleによってステップ数違うけどどこで制御するか... Facadeをどっかに設けたい

・リーダーやメンバエージェントのリストは設けず，全エージェントのリストだけ用意する．
　ただしその格納の順番をリーダー→メンバ→役割なしとし，各役割のエージェント数を数えることでインデックスを参照するときに一工夫してアクセスする．
・IDから座標への逆引きがやっぱ必要?
　→　座標の情報をエージェントに持たせることに

・ManagerとAgentやTaskなどの初期化処理はクラスを分けよう

? exitで終了するメソッドのテストがわからない...